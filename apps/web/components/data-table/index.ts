/**
 * Data table — architecture skeleton.
 *
 * Full implementation in Phase 8 (Analytics & Admin tables).
 * Exports established here so imports resolve before implementation.
 *
 * Will use @tanstack/react-table under the hood when implemented.
 * Install when needed: npm install @tanstack/react-table
 */

// Placeholder exports — replaced with full implementation in Phase 8
export { DataTable }         from "./data-table";
export type { ColumnDef }    from "./types";
export { createColumnHelper } from "./types";
